post:
  tags:
    - verification
  summary: Actor whitelisting
  description: |
    A verifier send a whitelisting request approving or denying to an actor.
  operationId: createVerificationRequest
  produces:
    - application/xml
    - application/json
  parameters:
    - in: body
      name: body
      description: Whitelisting request object
      required: true
      schema:
        $ref: '#/definitions/record:com.oceanprotocol.core.keeper.models.Whitelisting'
  responses:
    '201':
      description: successful operation
      schema:
        $ref: '#/definitions/record:com.oceanprotocol.core.keeper.models.Whitelisting'
